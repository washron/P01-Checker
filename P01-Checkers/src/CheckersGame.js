
let boardState = null; // To store the initial state of the board

    // Initialize game
    const game = new CheckersGame();
    const boardElement = document.getElementById('board');
    render();

    function createGameBoard() {
        constructor() {
            this.board = [];
        }
    }